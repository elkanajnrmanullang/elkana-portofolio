# Dependencies
/node_modules

# Build output
/.next
/out
/build

# Environment files (keep secrets safe)
.env
.env.*        # includes .env.local, .env.production, etc
!.env.example # allow template

# Vercel deployment config
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-debug.log*

# OS & Editor specific
.DS_Store
Thumbs.db
*.swp
*.swo
*.local

# Yarn PnP
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Coverage reports
/coverage

# SSL / Secret keys (prevent accidental exposure)
/*.pem
/*.key
/*.crt
